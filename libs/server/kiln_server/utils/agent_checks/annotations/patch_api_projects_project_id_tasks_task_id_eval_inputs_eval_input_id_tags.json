{
  "method": "patch",
  "path": "/api/projects/{project_id}/tasks/{task_id}/eval_inputs/{eval_input_id}/tags",
  "agent_policy": {
    "permission": "allow",
    "requires_approval": true,
    "approval_description": "Allow agent to retag eval inputs? Tags decide which slice an item falls into, so retagging changes what an eval scores. Ensure you backup your project before allowing agentic edits."
  }
}
