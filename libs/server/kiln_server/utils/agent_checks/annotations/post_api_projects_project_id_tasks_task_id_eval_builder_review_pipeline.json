{
  "method": "post",
  "path": "/api/projects/{project_id}/tasks/{task_id}/eval_builder/review_pipeline",
  "agent_policy": {
    "permission": "allow",
    "requires_approval": true,
    "approval_description": "Drive multi-turn synthetic-user conversations and run judge + claims on each? Invokes the target model, SU driver, and judge (cost)."
  }
}
