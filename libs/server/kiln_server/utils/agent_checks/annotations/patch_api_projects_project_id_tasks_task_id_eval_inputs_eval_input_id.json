{
  "method": "patch",
  "path": "/api/projects/{project_id}/tasks/{task_id}/eval_inputs/{eval_input_id}",
  "agent_policy": {
    "permission": "allow",
    "requires_approval": true,
    "approval_description": "Allow agent to edit eval input? Editing data re-drives multi-turn conversations and can invalidate prior comparisons. Ensure you backup your project before allowing agentic edits."
  }
}
