{
  "method": "patch",
  "path": "/api/projects/{project_id}/tasks/{task_id}",
  "agent_policy": {
    "permission": "allow",
    "requires_approval": true,
    "approval_description": "Allow agent to edit task? Ensure you backup your project before allowing agentic edits."
  }
}
