{
  "method": "patch",
  "path": "/api/projects/{project_id}/tasks/{task_id}/finetunes/{finetune_id}",
  "agent_policy": {
    "permission": "allow",
    "requires_approval": true,
    "approval_description": "Allow agent to edit fine-tune? Ensure you backup your project before allowing agentic edits."
  }
}
