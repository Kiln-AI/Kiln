{
  "method": "post",
  "path": "/api/jobs/evals/run",
  "agent_policy": {
    "permission": "allow",
    "requires_approval": true,
    "approval_description": "Run an eval in the background? This runs LLM calls across the eval's dataset split and uses AI credits."
  }
}
