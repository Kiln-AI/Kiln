{
  "method": "get",
  "path": "/api/projects/{project_id}/extractor_configs/{extractor_config_id}/run_extractor_config",
  "agent_policy": {
    "permission": "allow",
    "requires_approval": true,
    "approval_description": "Run document extractor? This processes all documents and may take significant time."
  },
  "request_body": null,
  "parameters": {
    "path": {
      "project_id": {
        "type": "string",
        "description": "The unique identifier of the project.",
        "title": "Project Id"
      },
      "extractor_config_id": {
        "type": "string",
        "description": "The unique identifier of the extractor configuration.",
        "title": "Extractor Config Id"
      }
    },
    "query": {
      "tags": {
        "anyOf": [
          {
            "type": "string"
          },
          {
            "type": "null"
          }
        ],
        "description": "Comma-separated list of tags to filter documents by.",
        "title": "Tags"
      }
    }
  }
}
