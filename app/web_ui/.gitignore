.DS_Store
node_modules
# Staging for setup_startup.sh's hardlink seed of node_modules. Cleared on both
# its paths; this covers a run that was killed mid-copy.
.node_modules.warm*
/build
/.svelte-kit
/package
.env
.env.*
!.env.example
vite.config.js.timestamp-*
vite.config.ts.timestamp-*

# Playwright
/test-results/
/playwright-report/
/blob-report/
/playwright/.cache/
/playwright/.auth/
/.e2e_home/
# Isolated HOME for .agents/scripts/dev_server.sh, same idea as .e2e_home.
/.agent_dev_home/
