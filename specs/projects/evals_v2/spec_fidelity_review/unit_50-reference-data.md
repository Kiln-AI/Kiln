# Spec-Fidelity Review: Unit 50-reference-data — Reference Data

Requirements: 22 total — MET 19, PARTIAL 0, MISSING 0, CONTRADICTED 0, DEFERRED_OK 3, CANNOT_VERIFY 0

---

## Requirements Table

| ID | Category | Verdict | Severity | Requirement | Spec Quote / Section | Evidence | Divergence |
|---|---|---|---|---|---|---|---|
| 50-R01 | data-model | MET | — | `EvalInput.reference` is typed `dict[str, JsonValue] \| None` with default `None` | §2 "Root type: `dict[str, JsonValue] \| None`" | `libs/core/kiln_ai/datamodel/eval.py:299` — `reference: dict[str, JsonValue] \| None = Field(default=None, ...)` | — |
| 50-R02 | data-model | MET | — | Root constraint: when non-None, root is a dict (not bare string/array/bool), enforced by the type annotation | §2.1 "Root constraint: When non-None, the root must be a dict ... enforced by the type annotation itself" | `eval.py:299` — Pydantic type annotation `dict[str, JsonValue] \| None` validates root shape | — |
| 50-R03 | design-decision | MET | — | Shape is a flat dict, NOT per-config namespaced sub-dicts | §2.2 table: "Per-config namespaced sub-dicts ... Tightly couples datasets to specific configs. ... Breaks dataset sharing." | `eval.py:299` — `dict[str, JsonValue]` (flat dict with string keys), no per-config nesting | — |
| 50-R04 | design-decision | MET | — | No EvalInput-creation-time validation of reference shape | §2.3 trade-off 1: "No EvalInput-creation-time validation of reference shape" | `eval.py:289-306` — No model_validator on EvalInput that validates reference contents/keys | — |
| 50-R05 | design-decision | MET | — | No dataset-level schema enforcement across EvalInputs (two EvalInputs may have different reference keys) | §2.3 trade-off 3: "No schema enforcement across EvalInputs in a dataset ... handles missing keys via the skip mechanism, not via dataset-level schema enforcement" | No schema validation code exists on EvalInput or parent Eval that checks reference keys match across items | — |
| 50-R06 | runtime | MET | — | JSON roundtrip guarantee via `JsonValue` type | §2.4 "Every value in `reference` must survive `json.dumps` / `json.loads` roundtrip ... enforced by the `JsonValue` type constraint" | `eval.py:299` uses `JsonValue` from Pydantic, which is `None \| bool \| int \| float \| str \| list[JsonValue] \| dict[str, JsonValue]` | — |
| 50-R07 | runtime | MET | — | One `EvalInput.reference` dict serves all EvalConfigs under the same Eval | §3.1 "One `EvalInput.reference` dict serves all EvalConfigs under the same Eval" | `eval_runner.py:460-462` — The same `EvalInput` (with its single `.reference`) is paired with each `EvalConfig` via the loop in `collect_tasks_for_eval_input` (lines 180-195, 204-218) | — |
| 50-R08 | runtime | MET | — | LLM judge types declare reference-key consumption via `required_var` list on `LlmJudgeProperties`, pre-checked with `extract()` | §3.2 "LLM judge types declare reference-key consumption implicitly through the Jinja2 `prompt_template` and explicitly through `required_var`" | `eval.py:87` — `required_var: list[str] = []` on `LlmJudgeProperties`; `v2_eval_helpers.py:78-91` — `check_required_vars` uses `extract()` to pre-check each var, returns `SkippedReason.extraction_failed` if undefined/None | — |
| 50-R09 | runtime | MET | — | If a `required_var` resolves to None or Undefined, case is skipped with `skipped_reason: extraction_failed` | §3.2 "If any evaluates to `None` or `Undefined`, the case is skipped with `skipped_reason: extraction_failed`" | `v2_eval_helpers.py:86-91` — returns `(SkippedReason.extraction_failed, f"required_var '{var_expr}' resolved to ...")` | — |
| 50-R10 | runtime | MET | — | Simple-check types (`exact_match`, `contains`, `set_check`) declare reference-key consumption via `reference_key: str` field | §3.2 "Simple-check types ... declare reference-key consumption via a typed `reference_key: str` field on their properties" | `eval.py:96` — `ExactMatchProperties.reference_key: str \| None`; `eval.py:129` — `ContainsProperties.reference_key: str \| None`; `eval.py:144` — `SetCheckProperties.reference_key: str \| None` | — |
| 50-R11 | runtime | MET | — | At runtime, if reference key is absent or `reference` is None, the case is skipped (C.runner.1) via `check_reference_key` | §3.2 "If the key is absent or `reference` is None, the case is skipped (C.runner.1)" | `v2_eval_helpers.py:48-75` — `check_reference_key` returns `SkippedReason.missing_reference_key` when `reference_data is None` (line 56-61) or key not in dict (line 62-67); called by `v2_eval_exact_match.py:29`, `v2_eval_contains.py:29`, `v2_eval_set_check.py:42` | — |
| 50-R12 | runtime | MET | — | `reference_key` is mutually exclusive with `expected_value` (for exact_match) / `substring` (for contains) / `expected_set` (for set_check), enforced by Pydantic `model_validator` | §3.2 "`reference_key` is mutually exclusive with `expected_value` ... Enforced by a Pydantic `model_validator`" | `eval.py:99-103` — `ExactMatchProperties.validate_value_source` checks exactly one of `expected_value`/`reference_key` is set; `eval.py:133-136` — `ContainsProperties.validate_value_source`; `eval.py:148-150` — `SetCheckProperties.validate_value_source` | — |
| 50-R13 | runtime | MET | — | `tool_call_check` and `step_count_check` do NOT consume reference data in V2.0 | §3.2 "Typed trace inspectors (`tool_call_check`, `step_count_check`) do not consume reference data in V2.0" | `v2_eval_tool_call_check.py` — no reference_data access, no `check_reference_key` import; `v2_eval_step_count_check.py` — no reference_data access, no `check_reference_key` import | — |
| 50-R14 | runtime | MET | — | Missing reference key causes skip (not hard failure); other EvalConfigs that don't need the key still run | §3.3 "The skip is not a hard failure -- other EvalConfigs that do not require the missing key still run against that EvalInput" | `eval_runner.py:460-484` — Each (EvalInput, EvalConfig) pair runs independently; a skip in one config does not prevent others from running. The `_run_v2_job` method runs one job at a time, and each job is independent | — |
| 50-R15 | runtime | MET | — | Skip uses structured `skipped_reason` + `skipped_detail` on EvalRun | §3.3 "A structured `skipped_reason` + `skipped_detail` is recorded on the EvalRun" | `eval.py:468-474` — `EvalRun.skipped_reason: str \| None` and `EvalRun.skipped_detail: str \| None`; `eval_runner.py:477-481` — runner writes `skipped_reason` and `skipped_detail` to EvalRun | — |
| 50-R16 | runtime | MET | — | `SkippedReason` enum includes `missing_reference_key` and `extraction_failed` | §3.3 "`skipped_reason: extraction_failed` ... `skipped_reason: missing_reference_key`" | `eval.py:257-258` — `SkippedReason.missing_reference_key = "missing_reference_key"` and `SkippedReason.extraction_failed = "extraction_failed"` | — |
| 50-R17 | data-model | MET | — | `EvalTaskInput` includes `reference_data: dict[str, JsonValue] \| None` mapped from `EvalInput.reference` | §6 "`EvalTaskInput.reference_data = eval_input.reference`. The field is renamed from `reference` to `reference_data`" | `eval.py:322-324` — `reference_data: dict[str, JsonValue] \| None = Field(default=None, ...)` on `EvalTaskInput`; `eval.py:370` — `from_eval_input` sets `reference_data=eval_input.reference` | — |
| 50-R18 | data-model | MET | — | `EvalTaskInput` has four reserved top-level names: `final_message`, `trace`, `reference_data`, `task_input` | §6 "reserved top-level names: `final_message`, `trace`, `reference_data`, `task_input`" | `eval.py:315-329` — exactly these four fields on `EvalTaskInput` | — |
| 50-R19 | data-model | MET | — | `EvalRun.reference_data: dict[str, JsonValue] \| None` field exists for storing reference data on run results | §6 + §3.3 (reference data is recorded on the EvalRun) | `eval.py:464-466` — `reference_data: dict[str, JsonValue] \| None = Field(default=None, ...)` on `EvalRun` | — |
| 50-R20 | design-decision | DEFERRED_OK | — | Corrected-output promotion is out of scope for V2.0 | §7 "Corrected-output promotion ... is out of scope for V2.0" | No corrected-output promotion code exists in `eval.py` or any eval adapter | — |
| 50-R21 | design-decision | DEFERRED_OK | — | Reference-key collision warning at EvalConfig creation time is tracked as a future item, not implemented in V2.0 | §3.4 "The OPENS.md item 'Reference-key collision warning' tracks the implementation of this warning" | No collision-warning code exists in EvalConfig validators or builder UX | — |
| 50-R22 | design-decision | DEFERRED_OK | — | No separate `criteria` field on `EvalInput`; per-case criteria expressed through reference data keys | §5.1 "There is no separate `criteria` field on `EvalInput`" | `eval.py:289-306` — `EvalInput` has only `data`, `reference`, and `tags` fields; no `criteria` field | — |

---

## Verifier-Added Items

(None. Re-scan of the spec did not identify any additional binding requirements beyond the 22 extracted above. The naming guidelines in §4 are conventions/best-practices, not runtime-enforced constraints, and the spec itself does not mandate runtime enforcement of naming conventions in V2.0.)
